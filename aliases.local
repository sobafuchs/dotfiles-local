# aliases vi to vim
alias vi="vim"

##########
# Docker
#########
function stop_all_docker_containers {
  docker stop $(docker ps -aq)
}

function remove_all_docker_containers {
  docker rm $(docker ps -aq)
}

source ~/workspace/knotch/dotfiles/aliases.local
